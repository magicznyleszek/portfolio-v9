{% include htmlBegin.liquid %}

    {% include partials/pageHeader.liquid %}

    <section id="pageContent" role="main">

        <h2>{% include helpers/categoryLabel.liquid category=page.category %}</h2>

        {% for post in site.posts %}
        {% if post.categories contains page.category %}
        {% include modules/postSummary.liquid %}
        {% endif %}
        {% endfor %}

    </section>

    {% include partials/pageFooter.liquid %}

{% include htmlEnd.liquid %}
