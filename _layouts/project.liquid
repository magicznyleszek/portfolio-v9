{% include htmlBegin.liquid %}

{% include global/pageHeader.liquid %}

<section role="main">
    <header i-pagetitle>
        <h1 i-pagetitle-name>{{ page.title }}</h1>

        <p i-pagetitle-description>{{ page.description }}</p>

        <time i-pagetitle-meta datetime="{{ page.date }}">
            {{ page.date | date: "%B %Y" }}
        </time>
    </header>

    <section i-projectContent>
        {{ content }}
    </section>

    {% include project/relateds.liquid older=page.previous newer=page.next %}
</section>

{% include global/pageFooter.liquid %}

{% include htmlEnd.liquid %}
