<nav gui-menu>

    <a
        gui-menu-item="{% if page.slug == 'projects' %}current{% endif %}"
        href="{{ site.baseurl }}/"
    >
        All
    </a>

    {% for category in site.data.categories %}
        <a
            gui-menu-item="{% if category.slug == page.slug%}current{% endif %}"
            href="{{ site.baseurl }}{{ category.url }}"
        >
            {{ category.label }}
        </a>
    {% endfor %}

</nav>
